apiVersion: v1
kind: Service
metadata:
  name: api-health-dashboard
spec:
  selector:
    app: api-health-dashboard
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
